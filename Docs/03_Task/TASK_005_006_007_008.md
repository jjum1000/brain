# 📋 Task 005-008: 기능 개선 작업들

**작성일**: 2025-10-31
**상태**: ⏳ 준비 완료

---

## Task 005: QR 코드 생성 기능

**우선순위**: 🟡 High | **예상시간**: 1-2일

### 목표
Vault 주소의 QR 코드 생성으로 사용자가 쉽게 입금할 수 있도록 함

### 파일 구조
```
src/components/common/QRCodeGenerator.tsx (신규)
```

### 의존성
```bash
npm install qrcode.react
```

### 구현 코드

**src/components/common/QRCodeGenerator.tsx**:
```typescript
import { FC } from 'react';
import QRCode from 'qrcode.react';
import { Button } from '@/components/ui/button';
import { Download } from 'lucide-react';

interface QRCodeGeneratorProps {
  value: string;
  title?: string;
  size?: number;
  level?: 'L' | 'M' | 'Q' | 'H';
}

export const QRCodeGenerator: FC<QRCodeGeneratorProps> = ({
  value,
  title = 'Solana Address',
  size = 200,
  level = 'H',
}) => {
  const qrRef = useRef<HTMLDivElement>(null);

  const downloadQR = () => {
    const canvas = qrRef.current?.querySelector('canvas');
    if (!canvas) return;

    const link = document.createElement('a');
    link.href = canvas.toDataURL('image/png');
    link.download = `qrcode-${Date.now()}.png`;
    link.click();
  };

  return (
    <div className="flex flex-col items-center gap-4">
      <div
        ref={qrRef}
        className="p-4 bg-white rounded-lg"
      >
        <QRCode
          value={value}
          size={size}
          level={level}
          includeMargin={true}
        />
      </div>

      <p className="text-xs text-slate-400">{title}</p>

      <Button
        onClick={downloadQR}
        variant="outline"
        className="gap-2"
        size="sm"
      >
        <Download className="h-4 w-4" />
        Download QR
      </Button>
    </div>
  );
};
```

### StrategyDetail에 추가
```typescript
import { QRCodeGenerator } from '@/components/common/QRCodeGenerator';

// DepositSection 내에 추가
<QRCodeGenerator
  value={strategy.vaultAddress}
  title="Vault Address"
  size={200}
/>
```

---

## Task 006: 성과 수수료 계산 로직

**우선순위**: 🟡 High | **예상시간**: 3-5일

### 목표
사용자 수익에서 플랫폼 수수료(20%)를 자동 계산하고 표시

### 파일 구조
```
src/lib/feeCalculator.ts (신규)
src/components/common/FeeBreakdown.tsx (신규)
```

### 구현 코드

**src/lib/feeCalculator.ts**:
```typescript
export interface FeeCalculationResult {
  totalProfit: number;
  performanceFee: number; // 20%
  netProfit: number;
  feePercentage: number;
}

/**
 * 성과 수수료 계산
 * 수익이 있을 때만 수수료 부과
 */
export function calculatePerformanceFee(
  totalProfit: number,
  feePercentage: number = 0.2 // 20%
): FeeCalculationResult {
  if (totalProfit <= 0) {
    return {
      totalProfit,
      performanceFee: 0,
      netProfit: totalProfit,
      feePercentage,
    };
  }

  const performanceFee = totalProfit * feePercentage;
  const netProfit = totalProfit - performanceFee;

  return {
    totalProfit,
    performanceFee,
    netProfit,
    feePercentage,
  };
}

/**
 * 누적 수수료 계산
 */
export function calculateCumulativeFees(
  earnings: number[],
  feePercentage: number = 0.2
): number {
  return earnings.reduce((total, earning) => {
    if (earning > 0) {
      total += earning * feePercentage;
    }
    return total;
  }, 0);
}

/**
 * 손익분기점 계산
 */
export function calculateBreakEvenProfit(
  targetNetProfit: number,
  feePercentage: number = 0.2
): number {
  // netProfit = totalProfit * (1 - feePercentage)
  // targetNetProfit = totalProfit * (1 - 0.2)
  // totalProfit = targetNetProfit / 0.8
  return targetNetProfit / (1 - feePercentage);
}
```

**src/components/common/FeeBreakdown.tsx**:
```typescript
import { FC } from 'react';
import { Card, CardContent } from '@/components/ui/card';
import { calculatePerformanceFee } from '@/lib/feeCalculator';

interface FeeBreakdownProps {
  profit: number;
  feePercentage?: number;
}

export const FeeBreakdown: FC<FeeBreakdownProps> = ({
  profit,
  feePercentage = 0.2,
}) => {
  const { performanceFee, netProfit } = calculatePerformanceFee(
    profit,
    feePercentage
  );

  return (
    <Card className="bg-slate-800/50 border-slate-700">
      <CardContent className="pt-6 space-y-3">
        <div className="flex justify-between text-sm">
          <span className="text-slate-400">Total Profit:</span>
          <span className="text-white font-mono">
            ${profit.toFixed(2)}
          </span>
        </div>

        <div className="flex justify-between text-sm border-t border-slate-700 pt-3">
          <span className="text-slate-400">
            Platform Fee ({(feePercentage * 100).toFixed(0)}%):
          </span>
          <span className="text-red-400 font-mono">
            -${performanceFee.toFixed(2)}
          </span>
        </div>

        <div className="flex justify-between text-sm border-t border-slate-700 pt-3">
          <span className="text-slate-300 font-semibold">Your Earnings:</span>
          <span className="text-green-400 font-semibold font-mono">
            ${netProfit.toFixed(2)}
          </span>
        </div>
      </CardContent>
    </Card>
  );
};
```

### Portfolio.tsx에 추가
```typescript
import { FeeBreakdown } from '@/components/common/FeeBreakdown';

// 수익 섹션 추가
<FeeBreakdown profit={userProfile?.stats?.totalEarnings} />
```

---

## Task 007: 거래 페이지네이션

**우선순위**: 🟡 High | **예상시간**: 2-3일

### 목표
대시보드의 거래 테이블에 페이지네이션 추가

### 파일 수정
```
src/components/pages/Dashboard.tsx (수정)
src/hooks/useTransactions.ts (수정)
```

### 구현 코드

**src/components/pages/Dashboard.tsx 수정**:
```typescript
import { useState } from 'react';
import {
  Pagination,
  PaginationContent,
  PaginationItem,
  PaginationLink,
  PaginationNext,
  PaginationPrevious,
} from '@/components/ui/pagination';

const ITEMS_PER_PAGE = 10;

export function Dashboard() {
  const { transactions } = useTransactions();
  const [currentPage, setCurrentPage] = useState(1);

  // 페이지네이션 계산
  const totalPages = Math.ceil(transactions.length / ITEMS_PER_PAGE);
  const startIndex = (currentPage - 1) * ITEMS_PER_PAGE;
  const endIndex = startIndex + ITEMS_PER_PAGE;
  const paginatedTransactions = transactions.slice(startIndex, endIndex);

  return (
    <div>
      {/* 기존 테이블 */}
      <Table>
        {/* ... 테이블 헤더 및 바디 */}
        <TableBody>
          {paginatedTransactions.map((tx) => (
            <TableRow key={tx.id}>
              {/* ... 행 내용 */}
            </TableRow>
          ))}
        </TableBody>
      </Table>

      {/* 페이지네이션 */}
      <div className="mt-6 flex justify-center">
        <Pagination>
          <PaginationContent>
            {currentPage > 1 && (
              <PaginationItem>
                <PaginationPrevious
                  onClick={() => setCurrentPage(prev => prev - 1)}
                  className="cursor-pointer"
                />
              </PaginationItem>
            )}

            {Array.from({ length: totalPages }, (_, i) => i + 1).map(page => (
              <PaginationItem key={page}>
                <PaginationLink
                  onClick={() => setCurrentPage(page)}
                  isActive={page === currentPage}
                  className="cursor-pointer"
                >
                  {page}
                </PaginationLink>
              </PaginationItem>
            ))}

            {currentPage < totalPages && (
              <PaginationItem>
                <PaginationNext
                  onClick={() => setCurrentPage(prev => prev + 1)}
                  className="cursor-pointer"
                />
              </PaginationItem>
            )}
          </PaginationContent>
        </Pagination>
      </div>
    </div>
  );
}
```

---

## Task 008: 전략 정렬 기능 (TVL, 수익률)

**우선순위**: 🟡 High | **예상시간**: 2-3일

### 목표
Strategies 페이지에서 TVL, 수익률 기준 정렬 추가

### 파일 수정
```
src/components/pages/Strategies.tsx (수정)
src/types/firestore.ts (확인)
```

### 구현 코드

**src/components/pages/Strategies.tsx 수정**:
```typescript
import { useState, useMemo } from 'react';

type SortBy = 'popular' | 'tvl' | 'roi' | 'risk';

export function Strategies() {
  const [sortBy, setSortBy] = useState<SortBy>('popular');

  // 정렬 로직
  const sortedStrategies = useMemo(() => {
    const sorted = [...strategies];

    switch (sortBy) {
      case 'tvl':
        return sorted.sort((a, b) => (b.totalValueLocked || 0) - (a.totalValueLocked || 0));

      case 'roi':
        return sorted.sort((a, b) => (b.currentROI || 0) - (a.currentROI || 0));

      case 'risk':
        return sorted.sort((a, b) => {
          const riskOrder = { low: 1, medium: 2, high: 3 };
          return riskOrder[a.risk] - riskOrder[b.risk];
        });

      case 'popular':
      default:
        return sorted.sort((a, b) => (b.followers || 0) - (a.followers || 0));
    }
  }, [strategies, sortBy]);

  return (
    <div>
      {/* 정렬 드롭다운 */}
      <Select value={sortBy} onValueChange={(val) => setSortBy(val as SortBy)}>
        <SelectTrigger className="w-40">
          <SelectValue />
        </SelectTrigger>
        <SelectContent>
          <SelectItem value="popular">Most Popular</SelectItem>
          <SelectItem value="tvl">Highest TVL</SelectItem>
          <SelectItem value="roi">Best ROI</SelectItem>
          <SelectItem value="risk">Low Risk First</SelectItem>
        </SelectContent>
      </Select>

      {/* 정렬된 전략 표시 */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-6">
        {sortedStrategies.map((strategy) => (
          <StrategyCard key={strategy.id} strategy={strategy} />
        ))}
      </div>
    </div>
  );
}
```

### Strategy 타입 확인
**src/types/firestore.ts**에 필요한 필드:
```typescript
export interface Strategy {
  id: string;
  name: string;
  totalValueLocked?: number; // TVL (단위: SOL 또는 USD)
  currentROI?: number;        // 현재 수익률 (%)
  followers?: number;         // 팔로워 수
  risk: 'low' | 'medium' | 'high';
  // ... 기존 필드들
}
```

---

## 📝 공통 주의사항

### 1. Firestore 데이터 구조
모든 작업은 Firestore의 기존 컬렉션 구조를 활용합니다.
필드 추가 시 마이그레이션 고려.

### 2. 성능 최적화
- useMemo 활용으로 불필요한 재계산 방지
- 정렬/필터링은 클라이언트 사이드 수행 (데이터 작을 경우)

### 3. 사용자 경험
- 로딩 상태 표시
- 에러 메시지 명확히
- 빈 상태 처리

### 4. 타입 안전성
- 모든 컴포넌트에서 TypeScript strict 모드
- 옵션 필드는 `?` 마크

---

## 🧪 검증 체크리스트

**Task 005**:
- [ ] QR 코드 생성 정상
- [ ] 다운로드 기능 작동
- [ ] 반응형 크기 조정

**Task 006**:
- [ ] 수수료 계산 정확
- [ ] 손익분기점 계산 정확
- [ ] UI 표시 정상

**Task 007**:
- [ ] 페이지네이션 정상
- [ ] 이전/다음 버튼 동작
- [ ] 페이지 번호 링크 정상

**Task 008**:
- [ ] 정렬 드롭다운 작동
- [ ] 각 정렬 방식 정상
- [ ] 성능 문제 없음

---

**작성**: Claude AI
**최종 검토**: 2025-10-31
**상태**: 실행 준비 완료 ✅
