# 📋 Task 009-015: 테스트, 최적화, 배포 작업들

**작성일**: 2025-10-31
**상태**: ⏳ 준비 완료

---

## Task 009: Unit Test 작성 (vitest)

**우선순위**: 🟡 High | **예상시간**: 3-5일

### 목표
핵심 로직 및 훅에 대한 유닛 테스트 작성

### 의존성
```bash
npm install -D vitest @testing-library/react @testing-library/user-event @vitest/ui
```

### 파일 구조
```
src/
├── hooks/
│   └── __tests__/
│       ├── useAuth.test.ts (신규)
│       ├── useStrategies.test.ts (신규)
│       └── usePortfolio.test.ts (신규)
├── lib/
│   └── __tests__/
│       ├── errorHandler.test.ts (신규)
│       └── feeCalculator.test.ts (신규)
└── vitest.config.ts (신규)
```

### vitest.config.ts
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

### 테스트 작성 예제

**src/lib/__tests__/feeCalculator.test.ts**:
```typescript
import { describe, it, expect } from 'vitest';
import {
  calculatePerformanceFee,
  calculateBreakEvenProfit,
} from '@/lib/feeCalculator';

describe('feeCalculator', () => {
  describe('calculatePerformanceFee', () => {
    it('should return 0 fee for 0 profit', () => {
      const result = calculatePerformanceFee(0);
      expect(result.performanceFee).toBe(0);
      expect(result.netProfit).toBe(0);
    });

    it('should return 0 fee for negative profit', () => {
      const result = calculatePerformanceFee(-100);
      expect(result.performanceFee).toBe(0);
      expect(result.netProfit).toBe(-100);
    });

    it('should calculate 20% fee correctly', () => {
      const result = calculatePerformanceFee(1000);
      expect(result.performanceFee).toBe(200);
      expect(result.netProfit).toBe(800);
    });

    it('should support custom fee percentage', () => {
      const result = calculatePerformanceFee(1000, 0.1); // 10%
      expect(result.performanceFee).toBe(100);
      expect(result.netProfit).toBe(900);
    });
  });

  describe('calculateBreakEvenProfit', () => {
    it('should calculate required profit for target net profit', () => {
      const required = calculateBreakEvenProfit(800); // target net $800
      expect(required).toBe(1000); // need $1000 gross with 20% fee
    });
  });
});
```

### package.json 스크립트 추가
```json
{
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  }
}
```

---

## Task 010: E2E Test 작성 (Playwright)

**우선순위**: 🟡 High | **예상시간**: 5-7일

### 목표
주요 사용자 플로우에 대한 E2E 테스트

### 의존성
```bash
npm install -D @playwright/test
npx playwright install
```

### 파일 구조
```
e2e/
├── auth.spec.ts (신규)
├── strategies.spec.ts (신규)
└── portfolio.spec.ts (신규)
playwright.config.ts (신규)
```

### playwright.config.ts
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  timeout: 30000,
  webServer: {
    command: 'npm run dev',
    port: 5173,
    reuseExistingServer: !process.env.CI,
  },
  use: {
    baseURL: 'http://localhost:5173',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
  ],
});
```

### E2E 테스트 예제

**e2e/auth.spec.ts**:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Authentication Flow', () => {
  test('should display home page', async ({ page }) => {
    await page.goto('/');
    await expect(page).toHaveTitle(/YOLOSEUM/);
  });

  test('should navigate to strategies without login', async ({ page }) => {
    await page.goto('/');
    await page.click('text=View Leaderboard');
    await expect(page).toHaveURL(/\/leaderboard/);
  });

  test('should show login required for dashboard', async ({ page }) => {
    await page.goto('/dashboard');
    // 리다이렉트 확인 또는 경고 표시
    await expect(page).toHaveURL(/\//);
  });
});
```

**e2e/strategies.spec.ts**:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Strategy Browse', () => {
  test('should load strategies list', async ({ page }) => {
    await page.goto('/strategies');
    await expect(page.locator('text=Trading Strategies')).toBeVisible();
  });

  test('should filter strategies by category', async ({ page }) => {
    await page.goto('/strategies');
    await page.selectOption('select:has-option(text="Momentum")', 'momentum');
    // 필터 결과 확인
  });

  test('should navigate to strategy detail', async ({ page }) => {
    await page.goto('/strategies');
    const firstStrategy = page.locator('[data-testid="strategy-card"]').first();
    await firstStrategy.click();
    await expect(page).toHaveURL(/\/strategy\/[\w-]+/);
  });
});
```

### package.json 스크립트
```json
{
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  }
}
```

---

## Task 011: 다국어 지원 (i18n)

**우선순위**: 🟢 Medium | **예상시간**: 3-5일

### 의존성
```bash
npm install i18next react-i18next
```

### 파일 구조
```
src/
└── i18n/
    ├── i18n.ts (신규)
    ├── en/
    │   └── translation.json (신규)
    └── ko/
        └── translation.json (신규)
```

### src/i18n/i18n.ts
```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import enTranslation from './en/translation.json';
import koTranslation from './ko/translation.json';

const resources = {
  en: { translation: enTranslation },
  ko: { translation: koTranslation },
};

i18n.use(initReactI18next).init({
  resources,
  lng: localStorage.getItem('language') || 'en',
  fallbackLng: 'en',
  interpolation: {
    escapeValue: false,
  },
});

export default i18n;
```

### src/i18n/en/translation.json
```json
{
  "navigation": {
    "dashboard": "Dashboard",
    "leaderboard": "Leaderboard",
    "strategies": "Strategies"
  },
  "pages": {
    "dashboard": {
      "title": "Dashboard",
      "totalInvested": "Total Invested",
      "totalEarnings": "Total Earnings"
    }
  }
}
```

### src/i18n/ko/translation.json
```json
{
  "navigation": {
    "dashboard": "대시보드",
    "leaderboard": "랭킹",
    "strategies": "전략"
  },
  "pages": {
    "dashboard": {
      "title": "대시보드",
      "totalInvested": "총 투자액",
      "totalEarnings": "총 수익"
    }
  }
}
```

### 컴포넌트에서 사용
```typescript
import { useTranslation } from 'react-i18next';

export function Dashboard() {
  const { t, i18n } = useTranslation();

  return (
    <div>
      <h1>{t('pages.dashboard.title')}</h1>
      <select
        value={i18n.language}
        onChange={(e) => i18n.changeLanguage(e.target.value)}
      >
        <option value="en">English</option>
        <option value="ko">한국어</option>
      </select>
    </div>
  );
}
```

---

## Task 012: API 에러 재시도 로직

**우선순위**: 🟢 Medium | **예상시간**: 2-3일

**파일**: `src/lib/retryFetch.ts` (Task 002 참고)

추가 구현:
```typescript
// Jupiter API 호출 래핑
export const jupiterQuoteWithRetry = async (
  inputMint: string,
  outputMint: string,
  amount: number
) => {
  return fetchWithRetry(
    () => jupiterClient.getQuote(inputMint, outputMint, amount),
    3,
    1000
  );
};

// Firestore 데이터 로드 래핑
export const firestoreQueryWithRetry = async <T>(
  query: () => Promise<T>
) => {
  return fetchWithRetry(query, 3, 500);
};
```

---

## Task 013: Firestore 보안 규칙 검증

**우선순위**: 🟢 Medium | **예상시간**: 1-2일

### 파일: firestore.rules

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 사용자 데이터
    match /users/{userId} {
      allow read: if request.auth.uid == userId;
      allow write: if request.auth.uid == userId;
      allow create: if request.auth.uid != null;
    }

    // 공개 전략 목록
    match /strategies/{strategyId} {
      allow read: if true; // 누구나 조회
      allow write: if isAdmin();
    }

    // 거래 기록 (사용자만 자신의 거래 조회)
    match /transactions/{userId}/{transactionId} {
      allow read: if request.auth.uid == userId;
      allow write: if request.auth.uid == userId;
    }

    // 리더보드 (공개)
    match /leaderboard/{document=**} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // 트레이더 정보 (공개)
    match /traders/{traderId} {
      allow read: if true;
      allow write: if request.auth.uid == traderId;
    }

    // 헬퍼 함수
    function isAdmin() {
      return request.auth.token.role == 'admin';
    }

    function isAuthenticated() {
      return request.auth != null;
    }
  }
}
```

### 배포
```bash
firebase deploy --only firestore:rules
```

---

## Task 014: Sentry 에러 로깅 통합

**우선순위**: 🟢 Medium | **예상시간**: 2-3일

### 의존성
```bash
npm install @sentry/react
```

### src/lib/sentryConfig.ts
```typescript
import * as Sentry from "@sentry/react";

export function initSentry() {
  Sentry.init({
    dsn: process.env.VITE_SENTRY_DSN,
    environment: process.env.MODE,
    tracesSampleRate: process.env.MODE === 'production' ? 0.1 : 1.0,
    integrations: [
      new Sentry.Replay({
        maskAllText: true,
        blockAllMedia: true,
      }),
    ],
  });
}
```

### App.tsx에 통합
```typescript
import { initSentry } from '@/lib/sentryConfig';

initSentry();

export default function App() {
  return (
    <AuthProvider>
      <WalletConnector>
        <AppContent />
        <Toaster />
      </WalletConnector>
    </AuthProvider>
  );
}
```

### 에러 캡처
```typescript
try {
  // 코드
} catch (error) {
  Sentry.captureException(error);
}
```

---

## Task 015: README 및 문서 작성

**우선순위**: 🟢 Medium | **예상시간**: 2-3일

### 파일 목록
```
README.md (신규)
CONTRIBUTING.md (신규)
docs/
├── ARCHITECTURE.md (신규)
├── SETUP.md (신규)
└── API.md (신규)
```

### README.md 구조
```markdown
# YOLOSEUM - Decentralized Trading Platform

## Overview
- 프로젝트 설명
- 주요 기능
- 기술 스택

## Quick Start
- 설치 방법
- 실행 방법
- 환경 변수

## Project Structure
- 디렉토리 구조
- 주요 파일 설명

## Development
- 개발 환경 설정
- 테스트 실행
- 빌드 방법

## Contributing
- 기여 가이드
- 코드 스타일
- Pull Request 프로세스

## License
- 라이선스 정보
```

---

## 🧪 공통 검증 체크리스트

**Task 009**:
- [ ] 테스트 파일 작성 완료
- [ ] 모든 테스트 통과
- [ ] 커버리지 80% 이상

**Task 010**:
- [ ] E2E 테스트 작성 완료
- [ ] 모든 테스트 통과
- [ ] 주요 플로우 커버

**Task 011**:
- [ ] i18n 설정 완료
- [ ] 영문/한글 번역 완료
- [ ] 언어 전환 동작

**Task 012**:
- [ ] 재시도 로직 작동
- [ ] Exponential backoff 확인
- [ ] 실패 시 에러 처리

**Task 013**:
- [ ] 규칙 배포 완료
- [ ] 접근 제한 확인
- [ ] 테스트 통과

**Task 014**:
- [ ] Sentry 초기화 완료
- [ ] 에러 캡처 확인
- [ ] 대시보드에서 볼 수 있음

**Task 015**:
- [ ] 모든 문서 작성 완료
- [ ] 링크 정확성 확인
- [ ] 코드 예제 검증

---

## 📞 작업 선행 순서

```
Task 001-003 (Solana 블록체인)
    ↓
Task 004-008 (기능 추가)
    ↓
Task 009-010 (테스트)
    ↓
Task 011-014 (최적화)
    ↓
Task 015 (문서화 및 배포)
```

---

**작성**: Claude AI
**최종 검토**: 2025-10-31
**상태**: 실행 준비 완료 ✅
